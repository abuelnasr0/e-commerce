export enum DarkModeButtonSize {
  small,
  medium,
  large,
}

export type DarkModeButtonProps = {
  size: DarkModeButtonSize;
};

export type SmallDarkModeButton = {
  buttonWidth: string;
  botttunHeight: string;
  circleRad: string;
  tranlate: string;
};
